import React from "react";
import styled from "styled-components";

const NewestBlog = (props) => {
    console.log(props)
    return (
        <div>
            working
        </div>
    )
}

export default NewestBlog;